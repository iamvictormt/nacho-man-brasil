# Project instructions

- Use Next.js App Router, TypeScript and Tailwind CSS.
- Keep routes and layouts in src/app and reusable components in src/components.
- Preserve the existing design, content and assets when changing infrastructure.
- Run npm run lint, npm run typecheck and npm run build before delivery.
- Do not rewrite published Git history or force push.
